import java.util.Scanner;

public class CardTester {

	public static void main(String[] args) {

		// create a new deck of cards
		CardDeck deck = new CardDeck();
		Scanner scan = new Scanner(System.in);
		int response1;
		int total = 0;
		// traverse deck showing card's suit, rank and value
		/*
		 * while(deck.hasNextCard()) { Card c = deck.nextCard(); int cardValue =
		 * c.rank.getValue(); System.out.println(c.toString()+" - has suit "+c.getSuit()
		 * + " and rank "+c.getRank()+" and value  "+cardValue); }
		 */
		
	}

}